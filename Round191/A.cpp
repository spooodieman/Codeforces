#include <bits/stdc++.h>
using namespace std;
#define ll long long

const ll mod = 1e9 + 7;
const ll INF = LLONG_MAX >> 1;

void solve(){
    ll size;cin >> size;
    ll answer = 0;
    ll curr = 0;
    ll maxim = 0;
    vector<ll> vals(size);
    for(auto &x : vals) cin >> x;
    

}

int main(){
    ios::sync_with_stdio(false); cin.tie(nullptr);
    solve();
}